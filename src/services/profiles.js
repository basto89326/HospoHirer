/**
 * Fetch a filtered list of employee profiles.
 * @param {{ role?: string, location?: string, available?: boolean }} filters
 * @returns {Promise<null>}
 */
async function listProfiles(filters) {
  // TODO: implement Prisma query with filters
  return null;
}

module.exports = { listProfiles };
